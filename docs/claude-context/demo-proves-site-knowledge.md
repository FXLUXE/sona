---
name: demo-proves-site-knowledge
description: "Demos/widgets must VISIBLY prove the bot already knows the business (live opening hours + other real facts), not just answer in chat"
metadata: 
  node_type: memory
  type: project
  originSessionId: 590a389a-b8f3-461e-8dd0-367f156028fa
---

Across ALL surfaces (demo pages, embedded widget, dashboard preview), the product must **visibly show it already knows the prospect's business** — so a prospect instantly sees it's not a generic bot. Headline signal: the business's **opening hours shown in an integrated way** (e.g. a live "Open now / Closed" badge), plus at least one other real business-specific detail (address/phone/top service/branding) surfaced in the UI itself — not only inside chat answers.

**Why:** The demo is the sale. A prospect must SEE "it knows MY site" within seconds (hours, location, services, brand) to believe it works and convert. Generic-looking demos don't sell. Ties to [[demo-sites-must-be-icp]] (real small local businesses) and the auto-extracted `tenant.facts` (address/hours/phone/prices) + `autoBrandTenant` (logo/colour).

All 4 feature groups approved (hours badge; business header + contact strip; starter chips + greeting; source citations + "what I learned" panel). **Constraint (Daniel): must be ONE cohesive, properly-integrated design — NOT bolted-on, messy clutter.** Plan the UX first; build AFTER the eval pipeline.

**BUILT 2026-06-29 (all 4 groups, one cohesive pass) in `product/src/index.ts` `widgetJs()`:** `/widget.js` route now passes sanitized `theme.facts` (hours/phone/address/price; `clean()` strips `<>`+caps len). Welcome card gained a "Here's what I already know" strip = live Open/Closed badge + location + phone pills (client `parseHours`/`hoursStatus` handle full/abbrev/range/slash/comma day formats; reject garbage like 00:00-00:00 → neutral fallback, never wrong status). Header sub-line shows live "Open now/Closed now" when hours parse. Answers show subtle source citations (`srcLine`, gated on `!unsure`, http/https-only scheme guard). All fact/source text via textContent (XSS-safe). CRITICAL GOTCHA: regex backslashes inside the widget template literal MUST be doubled (`\\d`,`\\n`) or they corrupt at build — verified clean. Reviewed (typescript-reviewer): 2 findings fixed (js:-scheme href guard; citation appended to `m.bub` not flex `m.row`). Parser unit-tested vs all 7 real demo hour strings.

**Floating-widget parity + contact handoff + launch button (2026-06-29, after compact):** (1) **CORS bug fixed** — public widget endpoints (`/api/chat`, `/api/suggest/*`, `/api/feedback`, `/api/book`) had NO CORS, so the embeddable widget's JSON calls were blocked on any real customer domain (only same-origin `/demo` worked); added `hono/cors` (`origin:"*"`, no creds, scoped to those 4 routes). (2) **Facts card now shows on the FLOATING widget, not just embed** — `greet()` only called `welcome()`/`buildKB()` in EMBEDDED mode; floating just did `row('a',G)+chips()`. Now floating appends the "HERE'S WHAT I ALREADY KNOW" pills (open/closed badge + 📍 + 📞) after the greeting. (3) **Contact handoff** — when `answer()` is `unsure`, append a deterministic line built from tenant facts: "you can reach {name} directly: 📞 phone · ✉️ email · 🔗 booking" + "🕑 Open today (Day): HH:MM–HH:MM" (new server helper `todaysHoursLine`, best-effort, "" on parse-fail; splits on `[;,\n]`, accepts 2-letter day codes like "Mo"). Email capture KEPT (Daniel chose "contact info + keep email capture"). Gated strictly on `unsure` so eval escalation detection (keys on `unsure` flag at eval.ts:89) is UNAFFECTED — re-ran judge eval = **still 94% (370/395)**, halluc even dropped 7→4. (4) **`cleanUrl()` helper** (exported from lib) strips trailing junk (`",`) from legacy booking URLs — applied in handoff, extraction, and widget Book button (`safeBook`). (5) **Logo no longer stretched** — header `.sona-ava img` aspect-ratio check: if logo wider than 4.2:1, fall back to bell mark (title shows name anyway). (6) **Launch button fixed** (Daniel: "too small, blends into dark footer") — bigger (padding 16/24, 16.5px, svg 24), `box-shadow` white halo ring `0 0 0 4px rgba(255,255,255,.92)` so it stands out on ANY bg, subtle 3.4s attention pulse (RM-gated). All verified live in Chrome on richardward floating widget.

**Source citations upgraded (Daniel feedback "link the exact page + piece of info, not just the website"):** retrieval now carries each chunk's `document_id` (`retrieveScored`/`retrieve` survivors gained `documentId`; built from a content→doc map over the tenant's chunks). `answer()` returns the EXACT page URLs of the chunks actually used (relevance order, deduped, max 3) instead of a generic `documents` limit(5) list — each URL gets a `#:~:text=` text-fragment (`textFragment()` helper, ~9-word chunk prefix) that deep-links + highlights the precise sentence. Widget `srcLine` shows host+path (e.g. `richardward.com/about-1`), deduped by full path. CAVEAT: the sentence-highlight is best-effort — only lands when the LIVE page text still matches the crawl (fails on restructured pages / popups / text normalization); the exact-PAGE link always works. Verified live in Chrome on richardward demo.

**How to apply:** Surface extracted facts in the widget/demo chrome (open/closed badge, contact strip, brand theming, suggested-question chips seeded from real content, personalised greeting naming the business + a top service, accurate source citations). Front end + back end. Integrated and tasteful (see [[demo-animation-subtle]]). Depends on facts actually being extracted — keep the crawler's HTML-based fact/brand extraction intact (do NOT overwrite raw HTML with rendered text before extractFacts/autoBrandTenant). Related: [[sona-ux-redesign-needed]] [[demo-animation-subtle]]
