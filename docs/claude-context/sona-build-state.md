---
name: sona-build-state
description: Current build state + next-steps for the Sona product (venture/product). Read when resuming feature work.
metadata: 
  node_type: memory
  type: project
  originSessionId: 81e1d05a-bf2e-4d54-88fb-100628baa21c
---

**Sona** = the product in `venture/product/` (see [[venture-ai-website-assistant]]). Bun+Hono+Supabase(pgvector)+Gemini(free)/Anthropic(paid). Multi-tenant RAG chat widget. Core = `src/lib.ts` (brain), `src/index.ts` (Hono server), `schema.sql`, `scripts/demo.ts`. Typecheck: `cd product && bun run typecheck`.

State as of 2026-06-24 night: ALL 5 NEXT STEPS below now DONE (typecheck green, boot ok, auth guard returns 401/403). New files: `src/dashboard.ts` (self-contained admin SPA), `scripts/resync.ts`. New lib helpers wired: `getUser`/`userTenants`/`isMember`/`claimTenant`/`recentLeads`/`recentConversations`/`contentGaps`. Endpoints added to index.ts: `/api/book`, `/api/stats`, `/api/config`, `/api/me/tenants`, `/api/me/claim`, guarded `/api/t/:tenant/{stats,leads,convos,gaps,ingest}`, `/dashboard`. Dashboard = Supabase magic-link auth (browser supabase-js, SRI-pinned @2.45.4), JWT verified server-side, membership re-checked per read (service key bypasses RLS so API guard is the real gate). configWarnings now also warns on missing SUPABASE_ANON_KEY. Widget now themed per-tenant (brand_color/logo/greeting, server-validated vs self-XSS). Remaining polish ideas (not started): booking UI tab in dashboard, feedback thumbs wiring, Stripe checkout, scheduled resync cron.

**SECURITY/BUG FIX WAVE (2026-06-24 night):** Two audit subagents (security + correctness) reviewed the code. Applied + verified (typecheck rc=0, boot probe green): SSRF guard `safeFetch` (DNS-resolved private-IP block + redirect re-validation + timeout) on all ingest fetches; auth-gated the 3 unguarded public routes (`/api/ingest`, `/api/stats` → requireMember; `/api/book` → rate-limit+validate); `/demo/:tenant` XSS fixed via slug-charset regex; `escHtml` on lead-notify email; `sanitize`+cap on `system_extra`, validate `calLink` before it enters LLM prompt; atomic `claimTenant`; quota now returns 429 not 200; conversation-insert null guard; lead dedup per conversation; rate-limiter eviction; CSP/XFO/nosniff on `/dashboard`. Full log: `product/docs/superpowers/specs/2026-06-24-sona-security-bugfix-remediation.md`. **DEFERRED (need a migration or design call — do NOT silently apply):** conversations(tenant,session_id) unique constraint; match_chunks RPC return document_id for correct source attribution; RLS on usage_events+feedback; server-signed sessionId (quota bypass); move RAG out of system prompt block; claim-token for demo-led onboarding; /api/config CORS. Two more research docs now exist: `2026-06-24-sona-gtm-marketing-research.md` (GTM: reposition as "AI front-desk that books appointments", flat $49-99 + setup fee, demo-led + agency channel) and `2026-06-24-sona-deep-research-compendium.md` (20-wave product/UX/security/vertical research — pending at time of writing).

**BUILD WAVE 1 (2026-06-24 night, after deep research):** Applied + verified (typecheck rc=0, boot+widget probe green). Speed-to-lead: new `sendLeadSms` (Twilio) + `fireLeadWebhook` (CRM/Zapier) + `notifyLead()` fan-out, wired into lead capture AND recordBooking (replaced bare email send). Model-path safety: `resolveProvider(t)` routes regulated/pro/business tenants to Anthropic (non-training, BAA) instead of free Gemini. Denial-of-wallet: Gemini `maxOutputTokens:1024`. Auto-brand: `extractBrand`/`autoBrandTenant` pull theme-color+og:image/icon during ingest, set tenant brand_color/logo_url if unset (demo-winner). Widget mobile: input font-size 16px (no iOS zoom), panel `min(320px,92vw)`/`min(460px,70vh)`; widget.js now try/catch graceful-fallback so it never hard-fails on customer sites. New cfg/env: TWILIO_ACCOUNT_SID/AUTH_TOKEN/FROM (+ configWarnings partial-twilio check; .env.example updated). **Migration drafted, NOT run (user runs it):** `product/migrations/2026-06-24-feature-wave-1.sql` — adds tenants.lead_notify_sms/lead_notify_webhook/regulated, conversations(tenant,session_id) unique index, RLS+policies on usage_events+feedback, leads.conversation_id index. New columns read defensively so code no-ops if migration not yet applied. **BUILD WAVE 2 (2026-06-24 night) — trust/answer-quality core. DONE + verified (typecheck rc=0, DSAR 401 gated).** Grounded-or-escalate: `grounded` flag + no-context branch in answer() system prompt forbids stating facts when KB empty (greet-or-[UNSURE]); unanswered logging now keys off `!grounded`. Structured facts: tenant `facts` jsonb formatted into an always-present KEY FACTS context block (sanitized+capped), counts as grounding (cold-start answers for hours/address/pricing). DSAR: `exportSubject`/`deleteSubject` lib helpers + guarded `POST /api/t/:tenant/gdpr/export|delete` (by visitor email across leads→conversations→messages→bookings). Migration appends `tenants.facts jsonb` (same file 2026-06-24-feature-wave-1.sql). DEFERRED in this wave: sandboxed-iframe widget rewrite (large/risky — flagged, not done). **BUILD WAVE 3 (2026-06-24 night) — revenue UI. DONE + verified (typecheck rc=0, settings 401-gated, widget Book button + dashboard Settings tab confirmed via boot probe).** Widget in-chat booking: validated calLink (booking_enabled) → "📅 Book" link in widget header opening Cal.com. ROI stats: tenantStats now returns leadValue + estimatedPipeline (leads×lead_value) + delivered (count usage_events kind LIKE 'notify:%'); notifyLead meters each channel as `notify:<channel>` (delivery log via existing usage_events, no new table). Dashboard: overview cards now show Bookings/Est. pipeline/Alerts delivered; NEW Settings tab (`renderSettings`) — owner edits lead_value, brand_color, booking_enabled+calLink, lead_notify_email/sms/webhook, regulated, key facts (label:value lines→jsonb). Server: `getTenantSettings`/`updateTenantSettings` (whitelisted+validated: https-only urls, color regex, numeric clamp, facts must be object) + guarded `GET`/`PATCH /api/t/:tenant/settings`. Migration appends `tenants.lead_value numeric default 0`. **Settings tab is the enabler** — without it wave 1-3 features had no UI to configure. ALL 3 build waves + 4 research docs done. **BLOCKER: user must run `product/migrations/2026-06-24-feature-wave-1.sql` against Supabase before waves 1-3 work end-to-end** (new columns: lead_notify_sms/webhook, regulated, facts, lead_value + RLS + unique index). Session cost hit ~$150 (flagged).

**REVIEW LOOP (2026-06-24 night) — adversarial find→verify→fix over all wave 1-3 code. 8 confirmed defects fixed + verified (typecheck rc=0, boot green):** (1) P1 lead-dedup .or() PostgREST injection — formatted phones broke the query; rewrote to fetch prior leads + dedup in JS. (2) P1 SSRF in fireLeadWebhook — tenant webhook URL skipped safeFetch; now routed through it. (3) P1 deleteSubject missed unanswered_questions (GDPR erasure incomplete) — added delete. (4) P2 exportSubject missed unanswered_questions — added to export. (5) P2 extractBrand logo https-only — now normalizes protocol-relative + http→https. (6) P2 dashboard loadTab missing await on renderOverview/renderSettings (errors swallowed) — added await. (7) P2 calLink validated by 3 different regexes — unified into exported `validCalLink` used by settings/answer/widget. (8) P2 persona+system_extra read by bot but no Settings UI — added tone select + extra-instructions textarea + included in PATCH. Review workflow: wf_bc5b01c2-4b6.

**LIVE SETUP (2026-06-24 night):** Supabase project qgjjnnuscmvhusndrtyq wired — schema.sql + migration run (verified: all 5 new tenant cols present, RLS on usage_events/feedback/chunks on, connection OK with new-format keys sb_publishable_/sb_secret_). .env now filled (URL + anon=publishable + service=secret + GEMINI_API_KEY). **CODE FIX: embed() switched from retired text-embedding-004 → gemini-embedding-001 with outputDimensionality:768** (keeps vector(768) schema; cosine handles un-normalized). Embeddings verified working live (768 dims). **RESOLVED: full pipeline LIVE.** User enabled Gemini billing (£10). gemini-2.0-flash was BOTH quota-capped AND then 404 retired → **chatGemini model switched to gemini-2.5-flash** ($0.30/$2.50 per M). Verified live: embed ✅ 768 dims, chat ✅ "PONG". So embeddings=gemini-embedding-001@768, chat=gemini-2.5-flash, both working on the user's key+billing. App is fully functional end-to-end (DB+auth+embed+chat all green). Pricing math: Gemini 2.5 Flash ~$0.0023/msg vs Claude Haiku 4.5 ~$0.0065/msg; at Pro tier ($79) Haiku ~$78/mo tokens kills margin so Gemini is correct default (Claude = premium/regulated only). Cost-saver not yet done: prompt caching + fewer chunks (~halve cost).

**DASHBOARD REDESIGN (2026-06-24 night, builder+reviewer subagents + superpowers:frontend-design):** Fully rewrote src/dashboard.ts (same dashboardHtml(base) signature, self-contained inline HTML/CSS/JS, security helpers esc()/safeHref()/SRI preserved verbatim, all API contracts unchanged). New: polished indigo→violet SaaS UI; **guided 3-step first-run wizard** (paste website → auto-derive slug + claim w/ 409 retry → ingest w/ progress → live bot preview iframe → copy embed); sidebar nav; **in-dashboard live Preview tab** (iframe to /demo/slug — no more typing slugs/URLs, which was the user's #1 complaint); ROI-framed overview; Settings keeps all fields. Reviewer subagent: no P0/P1, applied P2 defense-in-depth (filter tenants to slug regex on receipt, dashboard.ts:301). typecheck rc=0, serving 200 on :4000. Resolves [[sona-ux-redesign-needed]]. Server currently running via background bun process (bi96e1zg0) — not watch mode, so restart (`bun dev`) to pick up future edits.

**LAUNCH PLAN (2026-06-25, via superpowers writing-plans):** Full implementation plan at `product/docs/superpowers/plans/2026-06-25-sona-launch.md`. Locked from 30-question discovery (user answered ~8, delegated rest): Daniel wants LOW-TOUCH self-serve SaaS, no calls/meetings ever ([[daniel-low-touch-saas]]); niche = local-service appointment SMBs (I decide); lead handling = both self-book(Cal.com)+capture→OWNER follows up (never Daniel); sell within DAYS; must-work-first = demo wow + end-to-end lead capture + Stripe payment + live domain. **NEXT SESSION = execute the 4-task plan:** T1 Stripe billing (src/billing.ts; stripe_* cols already exist, likely no migration), T2 auto-demo generator + public landing (src/landing.ts + POST /api/demo — the no-calls acquisition engine), T3 deploy to real domain (Dockerfile→Railway), T4 end-to-end live verify. Plan has full code/steps. Verification = typecheck+boot+curl (no test framework, not a git repo). End of day 2026-06-24→25. SECURITY TODO: user pasted service_role secret + Gemini key in chat — rotate both before real use. Next: pick chat provider → smoke-test full chat flow → then Stripe billing. Remaining deferred/backlog: sandboxed-iframe widget, source-attribution RPC fix, server-signed sessionId, Stripe billing (original spec 2026-06-24-sona-v1-monetization-design.md still unbuilt).

**LAUNCH EXECUTION (2026-06-25, executing-plans skill) — T1+T2 code + T3 config SHIPPED + verified (typecheck rc=0, boot probe green on :4100).** New files: `src/billing.ts` (Stripe checkout/portal/webhook via `constructEventAsync`; `planLimits()` resolves from PLAN_LIMITS + optional tenant col overrides), `src/landing.ts` (`landingHtml(base)` — polished zero-external-script marketing+auto-demo page, all output esc'd; gradient-text logo fixed to solid per impeccable hook), `Dockerfile` (oven/bun:1, PORT 8080). `lib.ts`: cfg gained stripeSecret/stripeWebhookSecret/stripePrices{starter,pro,business}; configWarnings warns on missing Stripe; `ingestUrl` now enforces source limit inline (throws `SOURCE_LIMIT_REACHED` on NEW source over plan — re-ingest is free; resolved inline to avoid lib→billing circular import). `index.ts`: `GET /` now serves landing (was placeholder text), `GET /health`→ok, public rate-limited `POST /api/demo` (demo- prefixed slug, SSRF-guarded ingest), guarded `POST /api/t/:tenant/billing/{checkout,portal}`, unauth sig-verified `POST /api/billing/webhook`, both ingest routes catch limit→402 `{upgrade:true}` via `ingestError()`. `bun add stripe` → stripe@22.3.0. `.env.example` gained STRIPE_* + prod PORT/PUBLIC_BASE_URL notes. Probes: / 200+headline, /health ok, checkout unauth 401, webhook bad-sig 400, demo bad-url 400. **REMAINING CODE GAP (top next task): dashboard.ts has NO Billing tab** — checkout/portal endpoints exist but no Upgrade/Manage buttons in UI, so payment path unreachable from dashboard. **FOUNDER-GATED (Daniel must do, I can't): Stripe account→keys+create 3 products/prices, set STRIPE_* in host, Railway deploy (`railway up`) w/ all env, point domain + set PUBLIC_BASE_URL exact, add domain to Supabase Auth redirect allow-list, Stripe webhook endpoint→copy whsec, `stripe listen` local test, rotate leaked service_role+Gemini secrets, full live funnel verify (T4).**

**FEATURE + REDESIGN WAVE (2026-06-25, frontend-design skill + 2 parallel general-purpose agents) — SHIPPED + verified (combined typecheck rc=0; boot probe landing/dashboard/health all 200; landing screenshotted + critiqued via chrome-devtools MCP).** Server features I built (lib.ts+index.ts): widget 👍/👎 feedback (answer() now returns `messageId`) → public rate-limited `POST /api/feedback` → CSAT folded into tenantStats (`csat` %, `feedbackCount`); new lib helpers `recentBookings`/`conversationMessages`/`resolveGap`; new guarded endpoints `GET /api/t/:tenant/billing/status` (plan+limits+usage), `/bookings`, `/convos/:id/messages` (transcript), `POST /gaps/resolve`. Agent LAND fully redesigned `src/landing.ts` to **"concierge desk"** identity (palette ink #11212b / brass #c79a4b / warm paper #f6f1e9 / sage #3a5a50; Fraunces display + Inter body via Google Fonts <link>, NO external scripts; signature = brass nameplate "Sona/RECEPTIONIST/ON DUTY" + tactile bell that rings on submit; hero = working receptionist not a stat) + **heavy animation system** per user req (page-load choreography, IntersectionObserver scroll reveals, hover micro-interactions, ambient bell ring/shimmer/float, demo-arrival reveal, count-ups) all gated behind `prefers-reduced-motion`. I tweaked features grid to minmax(360px) → clean 2×2. Agent DASH extended `src/dashboard.ts`: **Billing tab** (plan badge + animated usage meters + Starter/Pro/Business upgrade cards → checkout redirect + Manage-billing portal; `billingEnabled:false` → quiet disabled notice; `?billing=success|cancel` toast) — **this closes the revenue-UI gap; payment path now reachable from dashboard**; Bookings tab; clickable conversation transcript modal; per-gap "Mark resolved"; CSAT overview card; reskinned to concierge palette by swapping :root var VALUES only; matching animation system (view transitions, count-ups, meter fills, skeletons) reduced-motion gated. NOTE: Bun has no hot-reload — restart `bun dev` to see edits. Brand now cohesive landing↔dashboard. **Still FOUNDER-GATED (unchanged): Stripe keys+products, Railway deploy, domain, Supabase redirect allow-list, webhook secret, rotate leaked secrets, live funnel (T4).** Session cost ~$65+ (subagent-driven).

**CONVERSION EXPANSION (2026-06-25, 3rd landing agent + me) — SHIPPED + screenshot-verified (typecheck rc=0, landing+demo 200, ~5600px page).** Landing.ts grown into full high-converting marketing page (still concierge-desk identity): sticky condensing header w/ persistent CTA, hero rotating verb, vertical trust strip (salons/clinics/dental/trades/studios/law/fitness/auto/real-estate/spas), 4 honest count-up stats, 3-step how-it-works w/ connecting line, loss-aversion "front desk that clocks out" ledger (Closes-when-you-do vs Open-whenever), keyboard-nav use-case tabs (Salon/Dental/Trades/Clinic example Q&A), 2×2 feature cards, native <details> FAQ accordion (6 Qs, no-JS safe), clean 4-up pricing (Pro highlighted), dark final CTA band, expanded footer. More motion (33 reveal elements, count-ups rebuilt w/ DOM nodes not innerHTML for XSS-safety), all reduced-motion + no-JS-fallback safe. CTAs at every section. HONEST: no fake testimonials/logos/ratings/counts — converts via value framing, loss-aversion (general truth), FAQ, transparent how-it-works, guarantee, live demo as proof. I separately rebuilt `/demo/:tenant` (index.ts) into a branded concierge stage (sticky bar, On-duty pulse, "Get this on my site →" CTA, animated chat nudge) — this is what prospects see in the landing's demo iframe = the buy moment.

**SECURITY LOOP + LAUNCH RESEARCH (2026-06-25, 2 parallel agents) — 5 confirmed findings FIXED + verified (typecheck rc=0, CSP headers present, feedback validates, demo guarded).** Adversarial security agent reviewed this session's new code; I fixed all: (P1) denial-of-wallet on public /api/demo — `ingestUrl` now caps text to MAX_INGEST_CHARS=120k + chunks to MAX_CHUNKS_PER_INGEST=80 (bounds paid embeds/request) + global `demoBudgetOk()` 120/hr backstop. (P1) rate-limit bypass via spoofable x-forwarded-for — new `clientIp(c)` trusts LAST XFF hop (proxy-appended) / Bun socket / anon, replaced all 4 raw-leftmost-XFF reads. (P2) feedback forgery — `recordFeedback` now requires messageId be a real uuid message belonging to tenant before insert (was: trusted body → CSAT skew for any tenant). (P2) demo-namespace poisoning — `/api/me/claim` rejects `demo-` slugs + `/api/demo` refuses owned tenants. (P2) missing CSP — added full CSP+nosniff to `GET /` and `/demo/:tenant` (frame-ancestors 'self' on demo so landing can iframe it). Agent CLEARED (no issue): XSS, Stripe plan/price tampering, webhook replay, IDOR on guarded routes, PostgREST injection, SSRF. Research agent wrote **`product/docs/superpowers/research/2026-06-25-sona-launch-readiness.md`** (grounded in real code). TOP LAUNCH BLOCKERS: (1) ROTATE leaked service_role+Gemini secrets; (2) create Stripe products/prices + register webhook (billing throws "unknown plan" until done); (3) Privacy Policy + ToS + cookie stance (Stripe+GDPR require); (4) demo denial-of-wallet [NOW PARTLY FIXED in code; still want demo-tenant TTL cleanup cron]; (5) email deliverability SPF/DKIM/DMARC for Resend `leads@sona.app`. TOP HIGH-ROI ADDS: annual plans (S, needs Stripe prices), weekly digest email (M, reuses tenantStats, needs Resend), expose `ingestSite` multi-page crawl UX (S, code-only), OG/social share image (S, code-only), exit-intent demo prompt (S, code-only). GOTCHA flagged: `resolveProvider` only routes paid/regulated tenants off training-eligible free Gemini IF ANTHROPIC_API_KEY set — blank in prod = paying customers silently on free tier that may train on their data.

**BIG BUILD WAVE 2 (2026-06-25, me + 2 landing/dash agents) — SHIPPED + verified (combined typecheck rc=0; all routes 200; screenshots confirm).** Driven by user "fix all insight issues + complete EVERYTHING" + live bug reports. (A) WIDGET fully redesigned in index.ts `widgetJs` — premium concierge (.sona- scoped styles, message bubbles, typing dots, brand-contrast text via lum(), bell avatar, Online-now pulse, brass send, "Powered by Sona" funnel link, kept 👍👎 + XSS-safe textContent); verified live — RAG gave correct grounded-escalate answer. (B) DEMO QUALITY — `/api/demo` now `ingestSite(slug,url,5)` (multi-page) so thin homepages get real content (fixes "google gave wrong/terrible answer"). (C) P0 BUG FIX (landing agent): "Ring the desk" flashed graphical issues — root cause `.desk-card` perpetual float animation + overflow:hidden clipping the demo iframe → per-frame GPU re-raster; fixed via `.desk.busy` stops animation while preview shown + iframe `isolation:isolate;contain:layout paint` + hardened handler (busy guard, clean error path). (D) LANDING humanised (landing agent): removed rotating verb (AI tell + misaligned), added first-person "note from the maker" (no contact, zero-touch), warmed copy, inclusive niche line "salons, clinics, studios, trades — any business that runs on appointments", OG/Twitter meta → /og-image.svg, exit-intent nudge (session-once, Esc, focus-trap, motion-gated), legal footer links. (E) LEGAL — new src/legal.ts → `/privacy` + `/terms` (Stripe/GDPR blocker; templates w/ [REVIEW] for company/jurisdiction). (F) DEMO TTL — `purgeOldDemos()` + scripts/purge-demos.ts (founder crons). (G) WEEKLY DIGEST — `sendWeeklyDigest`/`tenantsForDigest` + scripts/send-digests.ts (needs Resend). (H) OWNER MULTI-PAGE CRAWL — `POST /api/t/:tenant/crawl` (ingestSite, bound 1–30) + dashboard "Read my whole site" UI w/ 402→upgrade hint. (I) ANNUAL PLANS — cfg annual price slots + createCheckout maps `_annual`→base plan (limits stay correct) + dashboard Monthly/Annual toggle ("2 months free", live yearly totals). (J) /og-image.svg branded route. (K) PROVIDER GOTCHA — boot warning if paid/regulated tenants would use free Gemini w/o GEMINI_PAID=true or ANTHROPIC_API_KEY. Dashboard agent also humanised copy + resolved impeccable findings (no gradient text, contrast bumped). All animation reduced-motion gated; esc/safeHref preserved. Session cost very high (~$170, subagent-heavy). FOUNDER-GATED unchanged: rotate secrets, Stripe products/prices+webhook, deploy+domain, Supabase redirect allow-list, SPF/DKIM, [REVIEW] legal details. Cron scripts need a scheduler (Railway cron). Annual toggle needs STRIPE_PRICE_*_ANNUAL set.

**RAG QUALITY ROOT-CAUSE + VISUAL FIXES (2026-06-25, systematic-debugging, hands-on not delegated) — user frustrated bot gave "weak/unrelated answers" + "graphical errors" + "preview terrible".** DIAGNOSED via instrumented pipeline (scripts/diag-rag.ts, since removed): RAG is SOUND — Wikipedia control gave accurate grounded answer (23 chunks, sims 0.67-0.72). ROOT CAUSE = corpus quality + too-low threshold: `stripHtml` kept nav/menu/footer boilerplate, so thin/JS-heavy sites (google.com) yielded ONLY boilerplate (3 nav chunks, sims 0.52-0.58) → bot "answered" from menu text. FIXES (lib.ts, all verified): (1) `stripHtml` now drops nav/header/footer/aside/form/script/svg regions, prefers `<main>`/`<article>`, decodes entities → clean prose chunks. (2) retrieve() threshold 0.5→0.6 (evidence: real content 0.67+, boilerplate 0.52-0.58) → google.com now HONESTLY escalates ("leave your email") instead of weak answer; wikipedia still answers well. (3) Gemini 503/429 retry w/ backoff (user hit a 503). WIDGET fully redesigned earlier this session (premium concierge, verified live). PREVIEW redesigned: `/demo/:tenant` now a full-panel embedded chat (widget `embed=1` mode → fills #sona-mount, auto-greets, 3 suggested-question chips, slim brand bar + "Get this on my site" CTA) instead of sparse stage+corner bubble — screenshot-verified, much better. GRAPHICAL ERROR found+fixed: stat-band `.num` elements were EMPTY until IntersectionObserver fired (blank numbers on load) → now render static final values (< 2s / 24/7 / 0 / $0) as initial content, count-up animates over them. Mobile (390px) verified: no horizontal overflow, all sections render, stats show. Marquee hover-pause REMOVED (user wanted continuous scroll). Use-case section de-niched ("These are just examples — Sona works for any business people message before they buy"). NOTE: the "duplicate hero" the user/screenshots saw = the exit-intent modal (display:none default, fires on exit-intent, reuses hero card) caught mid-trigger by fullPage capture — NOT a flow bug; 1 h1 in DOM. Could remove exit-intent if it reads as spammy. LESSON: stop trusting agent "looks great" self-reports — verify with own screenshots + instrumentation (user was right). All typecheck rc=0.

**QUALITY PASS 3 (2026-06-25) — answer quality + brand pull + links + de-AI.** (1) RAG chunking rewritten: 900-word blocks → ~180-word OVERLAPPING chunks (stride 140) — standard high-impact precision fix (big chunks matched everything weakly). Demo crawl 5→8 pages. Combined with prior stripHtml + 0.6 threshold. (2) `extractBrand` made robust: theme-color + msapplication-TileColor; logo from og:image / apple-touch-icon / `<img...logo>` / icon link, with relative-URL resolution via `new URL(u,baseUrl)`; logo still validated `^https://[^"'<>]+$` before store AND re-validated in /widget.js before innerHTML (double-guard). autoBrandTenant + ingestUrl thread the page url. So demo pulls design (color+logo) from more sites during the crawl. (3) LINKS AUDIT (verified live): all 7 landing anchors (#top/desk/how/cases/services/faq/pricing) have matching IDs; /,/dashboard,/privacy,/terms,/og-image.svg,/health,/demo,/widget.js all 200; dashboard Privacy/Terms/lead page_url all safeHref'd. No broken links. (4) SECURITY (new surface since last full review): /api/t/:tenant/crawl guarded by requireMember + url-validated + maxPages bound 1-30; legal/og routes static+esc'd; widget embed chips use textContent; brand logo double-validated. No new flaws. (5) Removed the AI-tell "A note from the maker" founder-story section entirely (user: "sounds so AI it's not even funny") — orphan CSS left harmless; real personal note should be Daniel's own words. All typecheck rc=0. HONEST on answer quality: RAG is sound + improved, but quality is bounded by the customer site's actual content — answers well on content-rich sites, honestly escalates ("leave your email") on thin ones (google.com is pathological — a search box, no content). Further gains available: reranking, query expansion, hybrid search, boilerplate chunk filtering.

**RAG FINAL TUNE + CODE-COMPLETE (2026-06-25).** Added: light hybrid rerank in retrieve() (over-fetch k*3, boost chunks containing query keywords +0.02 each cap +0.06, keep 0.6 vector floor) + boilerplate-chunk filter at ingest (`looksLikeBoilerplate`: drops Title-Case nav runs w/ ~no sentence punctuation). Final live e2e PASSED: barber site → grounded "cut, dress, groom, style, shave hair or beards"; off-topic jet-ski → escalated; lead captured; stats counted. **CODE-COMPLETE for launch** — all code-only work done + verified (RAG quality, brand pull, widget, demo preview, billing, legal, GDPR, dashboard, crawl, digest, purge, security, links). REMAINING = FOUNDER-GATED ONLY (no code can do): rotate leaked service_role+Gemini secrets; Stripe account→keys+3 products/prices (+optional annual)+webhook whsec; deploy (Dockerfile→Railway) + domain + exact PUBLIC_BASE_URL; Supabase Auth redirect allow-list; SPF/DKIM/DMARC for Resend; set GEMINI_PAID=true; schedule scripts/purge-demos.ts + scripts/send-digests.ts (Railway cron); review [REVIEW] markers in src/legal.ts; write a real founder note if wanted (AI one removed). Session cost ~$400 (very subagent+screenshot heavy — flag to keep future sessions leaner).

**PROGRAMMATIC SEO + UI FIXES (2026-06-25).** New `src/seo.ts`: 6 hand-written differentiated vertical pages (salons/dental-practices/trades/clinics/fitness-studios/auto-shops) served at `/for/:slug` (unique headline+pain+3 example Q&A each, FAQ JSON-LD, canonical/OG, demo input, cross-links) + `/sitemap.xml` + `/robots.txt`, wired in index.ts (CSP'd, bad slug→404). Autonomous free inbound channel (ships with app, no accounts/spam). Removed maker's-note orphan CSS (note text already gone — user was on stale server; Bun no hot-reload). Fixed final CTA "closer" band: replaced the clashing white URL input with a single brass "Paste your site & see it free" button → #desk anchor (user: "horrid white box, doesn't fit"); closer JS already null-safe for removed #url2/#go2. RAG hybrid rerank + boilerplate-chunk filter added + verified (barber test: grounded answer, off-topic escalate, lead capture). All typecheck rc=0; closer + SEO routes screenshot/curl verified.

**DISTRIBUTION REALITY (told user honestly 2026-06-25):** User wants "AI gets clients for me, free, now, zero touch." HARD TRUTH given: (1) SEO = zero ONGOING touch but NOT free (needs deploy: ~$5/mo Railway + ~£10/yr domain) and NOT fast (weeks-months to rank) — it's a long-game asset, not instant distribution. (2) I (AI) CANNOT autonomously contact strangers — no ability to send email/post/DM without HIS connected accounts; "get it out to people" = distribution = needs a channel = his account, money, or time. No free fully-autonomous client-getting exists. (3) Cheapest realistic path: one-time deploy+domain (~£15 start), then personalized-demo outreach pipeline (I BUILD: scrape local businesses → auto-gen each a demo link → write personalized email; HE sends from own account — account-gated + UK PECR/GDPR + deliverability reality). Declined to build an unattended cold-email spam robot (deliverability/legal/ethics). NEXT IF HE WANTS: build the demo-outreach list+message generator script (ready-to-send personalized emails w/ demo links) — closest thing to "AI does it," pending his go + sending account.

**OUTREACH PIPELINE BUILT (2026-06-25) — verified end-to-end, $0 tool cost.** User: "create all these then we review before paying." Built: `src/outreach.ts` (cold-email copy: 2 subjects + body + follow-up; honest, opt-out "reply stop", PECR-aware, sender configurable Daniel/daniel@sona.app); `scripts/find-prospects.ts` (FREE local-business finder via OpenStreetMap Overpass — no API key, 3 mirror fallback + UA fix for 406, maps verticals→OSM tags, filters socials/dupes; TEST: found 67 Leeds salons w/ websites); `scripts/generate-outreach.ts` (pipeline: prospect list → ensureTenant+ingestSite per prospect → live demoUrl → buildOutreach → writes outreach.md review + outreach.csv mail-merge; bounded by limit default 5; TEST: generated Luxe+Yazz demos+emails, reads well). Campaign doc `product/docs/superpowers/marketing/2026-06-25-outreach-campaign.md` (play, how-to-run 2 commands, legal/deliverability reality, cost table, cadence, review checklist). Sample artifacts left in product/ root (prospects-salons.json, outreach.md, outreach.csv) for review — regenerable. All typecheck rc=0. FOUNDER PAYS ONLY WHEN SENDING: deploy+domain (~£15 start, set PUBLIC_BASE_URL — demo links are localhost until then), sending route (own inbox free / Instantly ~£30mo w/ separate warmed domain+SPF/DKIM), email-finding (OSM rarely has emails). I do NOT send — account-gated by design. Caveat surfaced: thin salon sites ingest few chunks → weak demos (content-reality, not a bug).

**UX FIXES + GUIDE (2026-06-25).** (1) DEMO SPEED: 20s hang fixed — /api/demo now ingestUrl(homepage) first → returns demo ~3s, then `void ingestSite(...,6)` enriches in background. (2) LEAD FILE: user confused (opened .ts source thinking it was leads). Added `GET /api/t/:tenant/leads.csv` (guarded, BOM+CSV) + dashboard "⬇ Download CSV" button in Leads tab (authed blob fetch→download). Guide clarifies leads live in dashboard+email/SMS, not files. (3) PDF GUIDE: `docs/sona-guide.md` (plain-English non-dev, 10 sections) → `scripts/md-to-pdf.ts` (MD→branded-HTML converter, concierge palette) → headless Chrome `--print-to-pdf` → `docs/sona-guide.pdf` (388KB, screenshot-verified NICE: Fraunces title+brass rule, styled tables/callouts). Chrome at "/c/Program Files/Google/Chrome/Application/chrome.exe"; no pandoc/weasyprint on box. (4) DEMO ENHANCE: embed greeting now "Hi — I'm [name]'s assistant, and I've just read this website…". All typecheck rc=0, boot-probe verified (leads.csv 401 guarded, dlLeads btn present, /for/salons 200). NOTE: 22 files modified this mega-session, ~$560 cost — future sessions leaner/targeted.

**OUTREACH TAB + DEMO LAYOUT (2026-06-25).** Built dashboard Outreach tab: lib `listDemoTenants()` (demo-% unowned tenants); endpoints `GET /api/outreach/demos` (lists demos + buildOutreach email per row) + `POST /api/outreach/build {urls}` (homepage-fast demo gen for pasted URLs, demoBudget-capped, both requireUser/401); dashboard nav 'Outreach' + renderOutreach/loadOutreachList (build textarea + table: business / Open demo / Copy email). Persists via existing tenants table (NO migration). Demo embed layout FIXED (user: "tiny logo + huge empty chat void, makes 0 sense"): embed now centers content to ~680px column (head/msgs/foot padding max(18px,calc(50%-340px))) + animates rows/chips in (sona-rise keyframe, RM-gated) — reads as a real centered chat, not edge-to-edge void. Verified screenshot. **SERVER OPS GOTCHA (important):** harness `run_in_background` bun servers do NOT die from `pkill -f bun` inside Bash (Windows process tree) → port 4000 stays held → restarts silently fail to bind & OLD code keeps serving (404s on new routes). FIX: kill via PowerShell `Get-Process bun | Stop-Process -Force` then start fresh. Server now clean on :4000 with all new code (outreach 401, Outreach nav present, dlLeads present). USER MUST REFRESH browser to see new tabs (Bun no hot-reload + they had old page loaded). All typecheck rc=0. Session ~$700, 24 files — flag leaner future sessions.

**DEMO MAJOR UPGRADE + OUTREACH SPEC (2026-06-25).** DEMO (embed) overhauled + screenshot-verified on ftmo.com: (1) brand NAME now extracted (og:site_name/title first-segment, TLD stripped → "FTMO" not "ftmo.com") + applied via autoBrandTenant; (2) TAILORED starter questions per business — `GET /api/suggest/:tenant` (LLM reads `sampleChunks` raw content, cached) → ftmo got "How do I claim 20% off/How are rewards withdrawn/free trials", luxe got salon Qs; widget welcome fetches + renders them; (3) CONTINUOUS ambient motion (user: one-shot entrance invisible) — brand-tinted drifting orbs (o1=their brand colour via ACC, o2=brass), floating+pulse-ring logo, gradient drift, all RM-gated; (4) demo speed: /api/demo homepage-first + bg enrich. GOTCHA: widget.js has cache-control max-age=300 → browser serves STALE widget after edits; must hard-reload (ignoreCache) to verify. OUTREACH TAB: 20-MCQ spec captured (full pipeline, all input methods, email+LinkedIn, autonomous-WITH-strict-quality-gates+manual-review-toggle+spam-safe-pacing, UK-wide any size, full-profile rows, full pipeline stages, auto-find emails, WARM HUMAN non-AI tone NO call/booking language, 2-3 follow-ups, reference-site-content personalization, as-fast-as-safe, win=signup, funnel metrics, build-all/queue/export, quality gates=demo-works+human-tone+valid-email+toggle). 10 target VERTICALS chosen (salons/dental/plumbers/electricians/physio/gyms/garages/estate-agents/accountants/vets) — UK-wide, NOT Swindon-scoped (user dropped Swindon 2026-06-25; finder = any-area). BUILT SO FAR: `src/outreach-store.ts` (JSON pipeline store, no migration), `src/outreach.ts` (warm human email + linkedinMessage, no-call). STILL PENDING (big): outreach finder endpoint (server-side Overpass by type+area) + build/list/status/funnel endpoints + dashboard Outreach tab overhaul (finder UI + pipeline-stage table + full profile + funnel + bulk + manual toggle) + seed Swindon prospects. HONEST LIMITS told user: autonomous SEND needs their email account (can't send from nothing); LinkedIn can't be automated (ToS) → generate text to paste. All typecheck rc=0. Session ~$870, 26 files.

**RAG PAGE-BUILDER RECALL FIX (2026-06-26, instrumented + verified) — biggest recall bug to date.** `stripHtml` blindly kept only `<main>`/`<article>`; WP/Wix page-builder themes wrap just the hero in `<main>` + lay real content in sibling `<section>`s → ~99% of text discarded. Real salon (laserclub.co.uk WP): 515KB HTML → 126 chars/1 chunk → bot said "I'm not sure" to everything. Fix in lib.ts: factored tag-strip into `toText()`, only trust `<main>` when `mainText.length >= fullText.length*0.5` (ratio guard not length guard) else keep full post-boilerplate page; + step-0 strip `<!--...-->` comments. RESULT: same salon → 17KB/42 chunks, answers services/hours/location/booking correctly (pricing honestly escalates, not on site); Wikipedia control unchanged (no regression). typecheck rc=0. THIS is the fix for "demos give weak answers" since the SMB niche (salons/clinics) is mostly WP/Wix. Full detail in [[demo-corpus-and-redesign]]. **CAVEAT (user must act): fix only helps NEW ingests — demos/tenants ingested before today have thin corpora → must be RE-INGESTED (purge chunks+documents, call `ingestSite`).** Next options when resuming: re-ingest live demos, or pivot to Outreach pipeline buildout (finder/build/list/funnel endpoints + dashboard Outreach tab — store `outreach-store.ts` + generators `outreach.ts` already built, only API surface + UI missing).

**DEMO RE-INGEST + OUTREACH PIPELINE WIRED (2026-06-26) — both shipped + verified (typecheck rc=0, all endpoints probed).** (A) Re-ingested niche demos with the 2026-06-26 stripHtml page-builder fix via new reusable `scripts/reingest-demos.ts` (purges chunks+documents per tenant, re-crawls root via ingestSite, reports before/after). Results: yazzhair 9→34, epiphany 20→26, laserclub 21→42 chunks; luxesalon stayed 2 (genuinely a thin one-page site — 53KB HTML but ~250 words real content, 0 crawlable links — still answers address/phone via facts block). Live QA (answer()): laserclub gives full treatment list (5 sources), yazz lists all 4 locations+services, luxe gives address+phone; epiphany "how to book" honestly escalates (no booking text on site). Also kept `scripts/inspect-corpus.ts` (per-tenant doc/chunk counts, flags THIN<5). NOTE: answer() sig = `answer(tenant,msg,{sessionId,pageUrl})` → returns `{reply,sources,unsure}` (not text/grounded). (B) Wired the previously-unused `outreach-store.ts` stage pipeline (found→built→ready→sent→opened→replied→signed→skipped) into a real founder-only Outreach feature. New `src/prospects.ts`: server-side OSM Overpass finder `findProspects(vertical,area,limit)` (10 verticals: salons/dental/plumbers/electricians/physio/gyms/garages/estate/accountants/vets; UK-wide, 3-mirror fallback, dedup, skips socials; Overpass endpoints HARDCODED so no SSRF) + shared `slugForUrl()` (deterministic demo- slug, dedup'd from /api/demo+build). index.ts: replaced old `/api/outreach/{demos,build}` with full pipeline — `GET /verticals` (public), `POST /find` (OSM→store stage found), `POST /build {slugs|urls}` (homepage-fast ingest+bg ingestSite→stage ready, demoBudget-capped, never overwrites owned tenant), `GET /list` (decorated: demoUrl+email/linkedin copy+QA flags demoOk≥3chunks/emailValid/humanTone), `GET /funnel`, `POST /stage {slug,stage,notes}` (manual progress toggle); all requireUser/401 except verticals. Removed now-unused listDemoTenants import. dashboard.ts renderOutreach fully overhauled: finder form (vertical select+area+max→Find), funnel summary cards, build (all-found + paste), pipeline table (business/area/demo+chunks/email+QA/copy email+LinkedIn/stage <select> onchange→/stage). All data esc()/safeHref()'d (XSS-safe per file pattern). Verified: verticals 200 (10), list/funnel/find/stage 401-unauth, dashboard 200; module test found 5 real Leeds salons→store→setStage→funnel all correct. Test store artifact deleted (clean slate). REMAINING for outreach: founder must deploy+set PUBLIC_BASE_URL (demo links localhost til then) + send from own inbox (no auto-send by design); LinkedIn = paste-only (ToS). Server NOT left running (bun no hot-reload; `bun dev` to pick up edits).

**RECALL + LOGIN FIXES (2026-06-26, hands-on) — both verified (typecheck rc=0, live answers correct).** User hit "where are you based?" → escalate on yazz demo despite address being on site. Diagnosed (instrumented retrieve): (a) cookie-consent banner text + a "403 Forbidden" page were ingested as chunks and ranked TOP for vague queries; (b) address chunk existed but buried mid-chunk, scored below 0.55 floor for short query "based?"; (c) extractFacts only read JSON-LD → WP/Wix salons (no JSON-LD) got NO address/phone in facts. FIXES in lib.ts: (1) `looksLikeBoilerplate` now also drops chunks matching `JUNK_PHRASE` (consent-manager + 403/404 stock text); (2) ingestUrl + ingestSite now SKIP non-2xx responses (`!res.ok`) so error-page bodies never ingest; (3) extractFacts gained a regex fallback over the FULL page text (tags stripped, header/footer KEPT) — UK postcode → address, tel: href / UK phone regex → phone — landing address in the always-present KEY FACTS block. RESULT: yazz "where are you based?" → "14 Otley Road, Guiseley, Leeds LS20 8HA" (facts), phone +441943870333; laserclub → address+hours+phone; junk chunks gone (yazz 34→30, laser 42→38 after re-ingest); honest escalate only where site truly lacks info (yazz hours). Re-ran `scripts/reingest-demos.ts`. NOTE: yazz address has minor prefix artifact ("1 HAIR STUDIO GUISELEY 14 Otley Road…") — correct + serviceable, regex grabs ~80 preceding chars. LOGIN: magic-link "did nothing on return" = silent failure. dashboard.ts hardened: (a) surfaces `?error=`/`#error=` callback params + does PKCE `exchangeCodeForSession` on `?code=`, shows the real reason on the login screen (was a blank loop); (b) added a **6-digit OTP code path** (`verifyOtp({email,token,type:'email'})`) that needs NO redirect → sidesteps the founder-gated redirect allow-list entirely. FOUNDER still should (either unblocks): Supabase Auth → URL Config set Site URL=`http://localhost:4000` + add redirect `http://localhost:4000/**`; OR add `{{ .Token }}` to the magic-link email template so the 6-digit code shows. Server left RUNNING on :4000 for user to view. Session cost very high (~$60, flag). **FOLLOW-UP: my login/outreach dashboard edits introduced 2 JS SYNTAX ERRORS that froze the dashboard on "Loading…" placeholder (all page JS dead).** Root cause + LESSON: the browser JS in dashboard.ts is a STRING inside a template literal → tsc does NOT syntax-check it AND backslashes get eaten by the template literal. `/\+/g` served as `/+/g` (invalid regex "nothing to repeat"); a literal `\n` in a textarea placeholder became a real newline mid JS-string ("invalid token"). FIX: use char classes (`/[+]/g`, `/[0-9]/`) not `\+`/`\d`, and `\\n` not `\n`, in any browser-JS string. Diagnosed via claude-in-chrome read_console_messages (exact line:col). After fix: console clean, dashboard renders fully — and user already has a valid Supabase session (logged in, nav tabs + Sign out visible), so the original "magic link froze" was ENTIRELY the syntax error, not auth/redirect. Verify any future dashboard.ts browser-JS edit by reading the live console, not just typecheck.

**OUTREACH DEMO QUALITY + EMAIL SCRAPE (2026-06-26) — verified, dashboard JS clean.** User saw finder-built demos showing GARBAGE: a Leeds salon demo (definitionhairdesign.co.uk) rendered as casino "alo8xyz" with betting questions; a barber demo showed brand name "96xrr1665624991982". ROOT CAUSE = stale OSM prospect data (these specific domains lapsed/were repurposed — definitionhairdesign is now a casino, therawdonbarbershop is parked) + demo took the PAGE's name not the verified OSM business name. FIXES: (1) lib.ts extractBrand rejects random-token names (`/^[a-z0-9]{6,}$/` w/ ≥2 digits, or >30% digits) → kills "96xrr…". (2) extractFacts now SCRAPES a contact email (mailto: first, else first plausible address in page text; skips asset/platform noise) → into facts.email — THIS is what gives outreach a recipient (OSM rarely has emails; verified live: yazz→yazzhair@live.co.uk). (3) new lib `finalizeDemo(slug,verifiedName)` forces tenant.name = verified OSM business name (overrides parked-page garbage) + returns pageName+email+content sample. (4) index.ts build endpoint: uses verified name, stores scraped email, and `siteMismatch()` flags lapsed/repurposed domains (no name-overlap AND no vertical keyword in content → "⚠ Live site looks unrelated… domain may have changed/expired. Review before sending."). VERTICAL_KW map per industry. (5) dashboard outreach table shows the ⚠ warning in red under the business name. VERIFIED on the 3 real demos: definitionhairdesign→name fixed + FLAGGED (casino), therawdonbarbershop→name fixed to "Rawdon Barber Shop" + OK, yazz→email scraped + OK. So broken demos now self-flag instead of embarrassing. NOTE: existing outreach-STORE rows from the user's earlier build still hold stale business/notes (my verify script fixed the TENANTS/DB but not the store) → user should re-Find+Build to refresh the pipeline view (new builds get full treatment). Tailored ready-to-send emails ALREADY existed (buildOutreach: subject+body+followup+linkedin per prospect, "Copy email" button) — they just lacked a `to` until email-scrape. ALSO added (earlier this turn): global top loading bar wired into api() (shows on every data pull) + .spin spinner + build "X of N" progress + Overpass too-broad guard (UK/country → instant helpful error, not 30s timeout). STILL PENDING from user msg: booking info surfacing + "calendar reader" (underspecified — asked user to scope; big feature). Cost CRITICAL (~$155+, flag hard — future sessions must be leaner/targeted, fewer restarts).

**DEAD-DOMAIN PURGE + BOOKING/CALENDAR + GUARDRAIL (2026-06-26) — user (rightly) angry that a flag wasn't enough: a lapsed salon demo (definitionhairdesign.co.uk) was STILL serving live casino "ALO8" content + casino suggested-questions, and luxe's name rendered raw "Luxe Salon Leeds &#8211; Hair Salon based in…". PROPER FIXES (all verified):** (1) extractBrand name now DECODES html entities first (new shared `decodeEntities()`) THEN splits on the brand separator incl spaced hyphen → "Luxe Salon" not the raw-entity full title. Also decode booking_url (Treatwell `&#038;`→`&`). (2) DEAD/REPURPOSED domains are no longer just flagged — the build endpoint now PURGES the demo (new lib `purgeTenant()` deletes chunks+documents+tenant) when `siteMismatch` fires, and parks the prospect stage "skipped" with the reason. So NO wrong-business/casino demo can be live or sent. Applied retroactively: definitionhairdesign PURGED (confirmed tenant gone; demo page now contains only "Sona" branding, zero casino). (3) BOOKING (user ask #1): extractFacts scrapes a booking link (cal.com/calendly/acuity/booksy/fresha/treatwell/setmore/timely/phorest/etc OR a "Book/Appointment/Reserve" anchor) → facts.booking_url + auto-enables tenant booking_enabled + booking_config.calLink → bot answers "how do I book?" with the link AND the widget 📅 Book button works. VERIFIED: yazz→book page, laserclub/luxe→Fresha, epiphany→Treatwell; bot gives the link. (4) CALENDAR READER (user ask #2 "read live calendar"): new lib `parseIcs()` + `calendarBrief()` — a tenant pastes a PUBLIC iCal URL into booking_config.ics; reader fetches (safeFetch, 5-min cache) + lists upcoming booked slots so the bot can answer availability. HONEST LIMIT told user: can't read a COLD prospect's private calendar (no auth) — only signed tenants who share a feed; bot-takes-the-booking deferred per user. NOTE: calendarBrief NOT yet injected into answer() prompt + no Settings UI field yet — engine built, wiring is the remaining piece. (5) GUARDRAIL (user demanded "stop making same mistakes"): new `scripts/check-dashboard-js.ts` renders dashboard+landing HTML, extracts every inline <script>, `new Function()`-parses each → catches the template-literal escaping bug class (e.g. `\+`→`/+/g`) that tsc CAN'T see; wired as `bun run check` (tsc + this). LESSON BURNED IN: verify RENDERED demo content (chunks/logo/questions), not just the DB name — a "name fixed" check passed while the demo still served casino. Email scrape also landed 4/6 (info@/hey@/yazzhair@live). Cost EXTREME this session (~$170+ — must go leaner). REMAINING: inject calendarBrief into answer() + add Settings iCal field + Settings booking_config.ics whitelist; consider /demo/:slug 404 for purged tenants (currently 200 empty shell).

**UAT ROUND 1 + BLOCKER FIXES (2026-06-26) — 21 findings logged in plan file `C:\Users\Daniel\.claude\plans\playful-meandering-emerson.md` (READ IT to resume UAT/fixes).** Daniel ran a 100-point UAT (MCQ-by-4 format). Key findings (full table in plan): #6 email-finding is CRITICAL (OSM has no emails, homepage scrape misses contact-page emails) — outreach pointless without it; #9 outreach copy too AI ("had a play with your website"); #15 SECURITY — Outreach tab/endpoints exposed to any signed-in CUSTOMER (only requireUser, not founder) → must gate founder-only; #5 build button not discoverable; #4 dashboard loses work on tab switch; #8 Settings need redesign (ugly/jargon/missing); #12 add Feedback tab; #11 [UNSURE] token leaked mid-reply; #14 loading bar wants bell motif; #13 gaps nav missing icon; #1/#16 header anchor + want FAQ nav link; #21 legal [REVIEW] still pending. **BLOCKER BATCH FIXED THIS TURN (user paused UAT for these; verified bun run check green, console clean, served):** #18 landing demo was a cramped 430px iframe → now opens as a near-fullscreen MODAL (`#result` → fixed overlay, `.result-card` min(1120px,94vw)×min(880px,88vh), backdrop+close+Esc+scroll-lock, mobile fills screen) so demo shows BIG; #20 re-run crash fixed via `closeDemo()` clearing `frame.src='about:blank'` before each build; #10 logo — `extractBrand` now prefers SQUARE marks (apple-touch-icon/img-logo/icon) over og:image, avatar CSS `object-fit:contain`+white bg+padding (no crop); #19 demo flashing — `#sona-mount` gets `isolation:isolate;contain:layout paint;overflow:hidden`; #7 demo page already flex-fits. NEXT: resume UAT from item 25 (demo preview/chat) to confirm modal, then the rest of the prioritized fix batch (email-finding P0 first, then copy, build UX, tab-state, settings redesign, feedback tab, founder-gate outreach, small fixes). Cost EXTREME (~$350 this session — MCQ UAT is pricey; future UAT = report fails by number).

**UAT BLOCKER BATCH 2 FIXED (2026-06-26) — verified.** (#26 CRITICAL chat MELTDOWN: bot repeated "I'm not sure about the cost of [X]" for EVERY question, ignoring the current one. Root cause `lib.ts answer()`: whole history flattened into one `convoText` blob as the user turn → model parroted its own prior replies. FIX: split prior turns (context) from current `msg`, anchor user turn as `Earlier in this chat… --- The visitor's CURRENT message — answer ONLY this…"<msg>"`, + system line "answer current message only, never repeat a previous answer/topic". VERIFIED multi-turn on laserclub: filling/hours/location/contact/off-topic each distinct + correct, escalations name the right topic.) Also: #11 [UNSURE] now stripped ANYWHERE (`includes` + `/g`), not just start. #23 demo header avatar `.sona-ava` → fit-to-image pill (height 34, width auto max 132, contain, centered) so wide logos show (was cramped 34px circle). #24 demo page body `height:100dvh;overflow:hidden` → no page scrollbar. #25 demo "Get this on my site" `.cta` → solid ink bg (#11212b) + paper text (#f6f1e9) for high contrast (was brass gradient + dark text, unreadable). All in plan file `playful-meandering-emerson.md`. Daniel re-testing UAT FROM ITEM 34. Still-open (deferred to full batch): #22 redirect-host (steam.co.uk→loaded.com built wrong demo+Everton logo), #6 email-finding (P0), #9 copy rewrite, #5 build UX, #4 tab-state, #8 settings redesign, #12 feedback tab, #15 founder-gate outreach (security), #13/#14/#16/#17/#1. NOTE cosmetic: opening_hours shows "10:00:00" (seconds) — trim later. Cost ~$455 (extreme).

**UAT FIX BATCH 3 (2026-06-26) — verified, bun run check green.** Full findings + plan: `playful-meandering-emerson.md`. FIXED: #35 login error now surfaces rate-limit ("Too many attempts — wait a few minutes") vs generic; #4/#33 dashboard no longer full-re-renders on window-focus (onAuthStateChange only acts on real SIGNED_IN/OUT, ignores TOKEN_REFRESHED); #9 outreach copy rewritten warm/human (dropped "had a play with your website" + salesy subjects); #6 KEY EMAIL FIX — root cause was build's `void ingestSite` (fire-and-forget) so only homepage scraped before reading email; added lib `scrapeEmail(html)` + `findContactEmail(baseUrl)` (sweeps homepage+/contact+/contact-us+/about+/book synchronously), wired into `/api/outreach/build` → VERIFIED yazz/epiphany/laserclub all return real emails; #15 SECURITY founder-gate — new `cfg.adminEmail` (env ADMIN_EMAIL), `requireAdmin`/`isAdminUser` on all `/api/outreach/*` (403 non-admin), `/api/me/tenants` returns `isAdmin`, dashboard hides Outreach nav for non-admins (TABS.filter); #31 opening_hours trims seconds (10:00:00→10:00); #13 added `gaps` nav SVG icon; #1 landing `#top` added to scroll-margin list (logo scroll clears sticky header). **ADMIN_EMAIL set in .env = danielheadsfx@gmail.com** (user's real signup email; I first wrong-guessed daniellheads@gmail.com then corrected). NOTE: without ADMIN_EMAIL set, Outreach is locked for EVERYONE. STILL OPEN (deferred, bigger/fresh session): #8 settings redesign, #12 feedback tab, #5 build-UX discoverability, #14 loading-bar bell polish, #16 FAQ nav link, #17 exit-intent, #22 redirect-host, #29 logo emoji validation, #30 skip-prospects-with-existing-chatbot, #32 lead-capture verify (demo leads on unowned tenant), #2 start-free copy. UAT blocked at item ~55 (Supabase OTP rate-limit lockout — wait/raise limit). Cost EXTREME (~$520).

**UAT OPEN-BACKLOG CLEARED (2026-06-26, model-tiered: 3 parallel Sonnet agents, 1 file each, Opus orchestrated — per [[cost-tiered-models]]).** All remaining code-only UAT items shipped + verified (combined `bun run check` green; boot probe :4000 → health/landing/dashboard 200, /api/t/:tenant/feedback + /api/outreach/list 401-guarded; boot log clean). landing.ts: #16 FAQ nav link, #2 "Start free"→"Create your free account" (4 CTAs), #17 exit-intent (Esc on document w/ open-guard, mouseout self-removes after fire, sessionStorage once/session). lib.ts+index.ts: #29 `looksLikeJunkLogo()` drops WP-emoji/gravatar/data-uri/placeholder logos in extractBrand; #30 `detectChatWidget()` (16 vendors) — build endpoint pre-fetches homepage, if widget found stages prospect "skipped (already has an assistant (<vendor>))" + records `chatbot`, skips demo; #22 redirect-host — build compares `safeFetch(...).url` host vs requested, records `redirectHost` on row + list; #6 `/api/outreach/stage` now accepts `{email}` (EMAIL_RE-validated) for manual fix; #12 `feedbackList(tenant)` helper + guarded `GET /api/t/:tenant/feedback` `{up,down,csat,items[{messageId,question,answer,rating,created_at}]}`; #32 lead-capture VERIFIED working (answer() lines ~953-984 insert lead on demo+owned, no change). dashboard.ts: #6 editable email `<input>` cell (POST stage {slug,email}, "no email — add one" placeholder) + chatbot/redirect flags under business name; #5 Build discoverability — "Build N demos →" CTA banner + per-row Build btn + scroll-to-results; #12 Feedback nav tab + `renderFeedback()` (up/down/CSAT cards + rated Q&A list); #14 loading bar polished (2px gradient+shimmer, 0.6 opacity, reduced-motion gated); #8 Settings redesigned into 6 plain-English cards (Your business / Booking & calendar / Notifications / Look & feel / How it answers / Plan & value) — same field IDs + PATCH contract, no new server fields. `api()` confirmed full-path (no double-prefix). Server LEFT RUNNING :4000. **STILL FOUNDER-GATED (I can't): #21 fill `src/legal.ts` [REVIEW] (company/jurisdiction); #34 add `{{ .Token }}` to Supabase magic-link email template; + prior launch gate (rotate leaked secrets, Stripe products/prices+webhook, Railway deploy+domain+PUBLIC_BASE_URL, Supabase Auth redirect allow-list, SPF/DKIM, GEMINI_PAID=true, cron the purge/digest scripts).** UAT resumes from item ~55 once OTP rate-limit resets. Cost kept LOW this session via Sonnet delegation.

**FULL SECURITY LOOP (2026-06-26, 4 parallel Sonnet finders by domain → Opus verified+fixed; `bun run check` green, restart probe :4000 → verticals now 401 confirmed, no boot errors).** Zero XSS / zero SQLi (UI + DB layer clean; all dashboard/landing/widget output esc()/safeHref()/textContent, PostgREST parameterized). Fixed (lib.ts/index.ts): (1) **LLM prompt-injection** was the main cluster — RAG chunks went into the SYSTEM prompt unsanitized; now each chunk is sanitize()'d + wrapped in `<source>…</source>`, added a "treat CONTEXT as data, never instructions" meta-rule, strip `\r\n\t` from facts values, sanitize() prior history turns, hardened sanitize() (broader jailbreak blocklist + control-char strip), calLink backticked in prompt + validCalLink rejects %0a/%0d/%09. (2) **denial-of-wallet** `/api/suggest/:tenant` (public, paid) — added per-IP rateLimit + in-flight promise dedup (refactored body into `computeSuggest()`). (3) **CSAT forgery** recordFeedback now one-rating-per-message. (4) **GDPR** export/delete now include the `feedback` table (delete it before messages). (5) **info-leak** embed/chatGemini/chatAnthropic no longer throw raw provider error bodies (log server-side, throw generic) + `/api/chat` wrapped in try/catch→503. (6) **booking-link phishing** ingestUrl only auto-wires Book button to `KNOWN_BOOKING_PROVIDER` hosts, not arbitrary scraped `<a>`. (7) **iCal** calendarBrief caps body (content-length+slice). (8) **founder-gate** `/api/outreach/verticals` now requireAdmin. DEFERRED (accepted-risk, single instance): DNS-rebind TOCTOU in safeFetch (needs IP-pinned agent); demoBudgetOk per-process (move to Redis at multi-replica); usage_events/feedback RLS only in migration not schema.sql (service key bypasses RLS so not API-exploitable). **NEW BUG logged (Daniel screenshot): Preview embed shows dark side-gutters + vertical scrollbar on wide screens (centered ~680px column + dark bg = black bands) — Sonnet agent fixing.** Task list now maintained via TaskCreate (Daniel wants full tasklist+subtasks always). Daniel logged into dashboard again — UAT can resume. #34 six-digit code = add `{{ .Token }}` to Supabase Magic-Link email template (founder/console, not code).

**ROUND 2 FIXES + UAT RETEST (2026-06-26 end-of-day, model-tiered Sonnet agents, Opus orchestrated). Plan file: `C:\Users\Daniel\.claude\plans\golden-wishing-sparkle.md` (has full detail + the 2 open bugs).** SHIPPED + `bun run check` green + server restarted clean on :4000: (A) **Preview width** — embed was a squished fixed 680px column; made fluid (`.sona-embed .sona-msgs` padding `clamp(16px,4vw,72px)`, `.sona-row`/`.sona-foot>*` max-width 680→1040, dashboard `.preview iframe` height 560→`min(74vh,780px)`) → **UAT PASS** (fills panel now). (B) **#13 DNS-rebind** closed in `safeFetch`/`assertSafeUrl` via **IP-pinning** (assertSafeUrl returns resolved addrs; safeFetch rewrites URL host to validated IP + sets `Host` header so no second unchecked DNS lookup; HTTPS verified still works via SNI/Host). Demo-budget left per-process (deferred, single instance). (C) **Outreach** — Clear-all (`POST /api/outreach/clear`+`clearAll()`), per-row Delete (`POST /api/outreach/delete`+`remove(slug)`), vertical/type **filter** dropdown, email-sweep wired into every build path, errors surfaced inline, list returns all rows (no cap), stage endpoint accepts email-only patch (`patchProspect`). All new endpoints requireAdmin (401 verified). Dashboard refresh bug (#15) fixed (URL-hash tab persist). Feedback tab (#12) shipped earlier this session. **UAT RETEST RESULTS (Daniel, live):** Preview ✅ · Garages find+filter ✅ (was a genuine empty-OSM result, not a bug) · Clear-all + Delete ✅ · **per-row Build ❌ STILL silent-nothing** · **email scraping ❌ still not pulling properly**. 

**⛔ 2 OPEN BUGS — START HERE NEXT SESSION (detail in plan file):** (1) **Per-row "Build →" does nothing** (no demo, no error). Handler `dashboard.ts loadOutreachList()` ~L972 posts `{slugs:[btn.dataset.slug]}` → `POST /api/outreach/build` (index.ts ~440, looks sound). DIAGNOSE FIRST via claude-in-chrome `read_console_messages` + Network response for `/api/outreach/build` (4xx vs JS-error vs `built:[]` empty). Suspects: onclick not re-bound after list re-render; build filters the job (owned/bySlug-miss/demoBudget); JS error pre-fetch. DON'T guess — instrument. (2) **Email scraping weak** — `findContactEmail`/`scrapeEmail` (lib.ts) miss obfuscated ("name [at] domain"), JS-form-only, and footer-variant emails; also email is downstream of bug #1 for per-row. Add stronger/obfuscated patterns; HONEST LIMIT: JS-form sites have no scrapable email → manual cell fallback / paid finder (Hunter/Apollo) is the only bulk fix. Maps to TASK #21 + reopened #6.

**TASK LIST STATE (TaskCreate/Update — Daniel wants a full tasklist maintained ALWAYS):** done #1-10,13,15-20; OPEN: #11 (founder: Supabase `{{ .Token }}` for 6-digit), #12 (founder: legal [REVIEW]), #14 (resume UAT from ~item 55 — Daniel IS logged in now), #21 (per-row Build — real bug), #6-reopened (email scrape). Server left RUNNING :4000 (restart `Get-Process bun|Stop-Process -Force; cd product && bun dev` — Bun no hot-reload, hard-refresh browser). Session cost very high again (~$180+ by mid-session) — Sonnet delegation kept it lower than full-Opus would; keep delegating mechanical work per [[cost-tiered-models]].

**SESSION 2026-06-27 (resume UAT + chatbot plan).** Started server clean on :4000, opened landing/dashboard/demo (demo slugs are `demo-` prefixed; best corpora `demo-laserclub-co-uk` 38ch, `demo-clarendondentalspa-co-uk` 33ch). Resumed UAT ~item 55. **9 NEW UAT FINDINGS #36–#44** (on TaskCreate list + plan file `C:\Users\Daniel\.claude\plans\wondrous-riding-puddle.md`; #36–#38 also in `playful-meandering-emerson.md`, #39–#44 appended there too): #36 dashboard STILL refreshes on tab away/back (reopens #4/#33 — a 2nd trigger beyond onAuthStateChange, likely visibilitychange/focus); #37 landing test widget >5s on youtube.com (latency); #38 MAJOR chatbot not smart; #39 landing demo re-run fails w/o refresh (widget state not torn down on 2nd build, related #20); #40 SECURITY+UX — client signup must land in CLIENT's own dashboard never owner's (verify fresh signup can't see owner tenants = P0 leak check) + TRIM client dashboard to client-only tabs (roles: Owner=Daniel admin, Clients=businesses, Customers=clients' visitors widget-only); #41 OWNER outreach finder — capitalise industry labels, free-text entry, multi-type filter, distance/radius (Overpass `around:`); #42 demos STILL won't build per-row from dashboard (open #21/#6 NOT fixed — diagnose console+Network on `/api/outreach/build` before fixing); #43 owner dashboard shows 0 everywhere (Overview/Leads/Convos empty — LIKELY demo chats write to demo- tenant not owner's tenant; verify where convos land); #44 dashboard visuals broken — Settings tab missing nav icon + all cards unstyled/no spacing/don't match concierge style (verify not stale-cache first). UAT NOT finished — Bookings/Feedback/Billing/Outreach tabs untested. **CHATBOT INTELLIGENCE PLAN written + approved (Plan subagent, grounded in real code): `C:\Users\Daniel\.claude\plans\chatbot-intelligence-overhaul.md`.** Diagnosis: "not smart" = OVER-ESCALATION (bot says "I don't know" to on-site answers), NOT model weakness. Key code facts found: retrieve() floor actually `0.55` not documented 0.6 (lib.ts:719); retrieval uses RAW current msg, ignores convo history (lib.ts:1064); NO query expansion/rewriting; weak hybrid (+0.06 keyword nudge AFTER floor); **source-attribution BUG** — `sources` = first 5 tenant docs unrelated to retrieved chunks + `match_chunks` RPC returns no document_id (schema.sql:143-152); multi-turn parrot bug already FIXED; no streaming/no prompt caching; embeddings gemini-embedding-001@768 unnormalized. **Plan: Phase 0 = build eval harness `scripts/eval.ts` + golden Q&A sets (~15 Qs/demo) to MEASURE answerable-recall as a NUMBER (e.g. 6/15→13/15) — the spine; Phase 1 quick wins (all on Gemini): tune floor from data, conversation-aware query, query expansion/multi-query, true tsvector keyword hybrid [needs migration], fix source attribution [needs migration], soften escalation w/ 1 clarifying Q, STREAM replies + parallelise answer() awaits, trim lead-path cost; reassess gate at ≥85% recall; Phase 2 structural only if needed (higher-dim embeddings, HNSW, structure-aware chunking, intent routing); Phase 3 polish.** DECISIONS LOCKED: quick wins first; **STAY ON GEMINI** (Claude deferred until paying customers fund per-msg cost); **Daniel chose to start the BUILD in a FRESH session** (this one hit ~92% context, ~$51). **NEXT SESSION ENTRY: open chatbot-intelligence-overhaul.md → Phase 0.1 eval harness first, then Phase 1.** Founder-gated in that plan: 2 SQL migrations (tsvector keyword index; match_chunks document_id fix); Phase-2 re-embed cost; Claude upgrade decision. Server left running :4000.

**SESSION 2026-06-29 — eval GATE PASSED + demo-proves-site-knowledge SHIPPED + widget polish. (Full detail: [[eval-pipeline-state]] + [[demo-proves-site-knowledge]].)** (A) Chatbot eval **94% recall (370/395), GATE PASSED** via embedding `taskType` (RETRIEVAL_QUERY/DOCUMENT) — see [[eval-pipeline-state]]. (B) All 4 demo-proves-site-knowledge feature groups built (hours badge, contact strip, starter chips, source citations) — exact-page citations w/ `#:~:text=` fragments. (C) THIS turn's fixes (all verified live in Chrome on `demo-richardward-com` floating widget, typecheck clean, eval re-confirmed 94%): **CORS** added (`hono/cors`, scoped to /api/chat,/suggest/*,/feedback,/book) — widget API calls were BLOCKED cross-origin on real customer sites, only same-origin /demo worked; **facts card now renders on FLOATING widget** not just embed (greet() floating branch appends buildKB pills); **contact handoff** — on `unsure`, answer() appends "reach {name} directly: 📞·✉️·🔗 + 🕑 Open today (Day): HH:MM–HH:MM" (new `todaysHoursLine`), email-capture KEPT, gated on unsure so eval-neutral; **`cleanUrl()`** strips legacy dirty booking URLs (`...metrospa",`) at handoff/extraction/widget Book button; **logo de-stretch** (header falls back to bell if logo aspect >4.2:1); **launch button** bigger + white halo ring (visible on ANY bg incl dark footer) + subtle 3.4s pulse (RM-gated) — fixed Daniel "too small/blends in"; **intro clears on first message** (floating send() now clears greeting/chips/KB, was only clearing .sona-welcome → cluttered chat); **cold-start warmup** on boot (retrieve+chat fire-and-forget) — first chat was ~20s cold hang, now 2.4s; **bubble overflow-wrap:anywhere** (long URLs were overflowing). DB cleanup of dirty calLink BLOCKED by shared-resource guard (not needed — cleanUrl sanitizes at use). Re-ran judge eval = still 94%, halluc 7→4.

**⛔ NEXT (Daniel's locked order this session): (1) BOOKING EPIC, then (2) #2 ICP gate.** (1) **In-widget booking — TIERED, auto-selected per business** (Daniel: "should be an option for ALL of these"): Tier-live = connected Calendly/Square/Acuity/Setmore API → real slots + instant confirm; Tier-real-slots = business shares iCal BUSY feed (we have `calendarBrief`/`parseIcs`) → free slots = busy∌opening-hours; Tier-request = polished date/time picker → name+email+preferred slot as LEAD, business confirms (DEFAULT, universal); Tier-link = bold "Book now" → their booking_url. Reading availability feasible via iCal busy feed (universal-ish) or provider API; WRITING confirmed booking needs provider API or their page. Build order: request-a-time calendar (~2-3h) → iCal real-slots → Calendly live → more providers. Onboarding stays self-serve (paste iCal / "Connect" OAuth, no calls). HONEST: a calendar not backed by real availability would double-book — never ship a fake one. (2) **#2 ICP gate** = wire "small independent local UK business only" filter into prospect-finder + onboarding (front+back), ~40-60min.

**DOMAIN (sidetask done):** `.com` taken for all Sona-words (secondary £77+). Pick = **`asksona.co.uk` £12.99/yr** ("Ask Sona" = widget says "Ask us", UK ICP on-brand, <£25). Alts: asksona.uk/trysona.co.uk/getsona.co.uk £12.99, asksona.net £19.99. Founder registers (I can't buy). **Servers left running: app :3939 (PUBLIC_BASE_URL=http://localhost:3939 PORT=3939), static preview :4040 serving scratchpad/host.html (floating-widget test page).** Session cost EXTREME (~$220).

**PLAN (2026-06-24 night, via superpowers brainstorming):** Full design + locked GTM decisions + next sub-project spec written to `product/docs/superpowers/specs/2026-06-24-sona-v1-monetization-design.md`. User delegated strategy calls ("you decide best"). Locked: V1 niche = local-service SMBs (marketing focus only, code stays generic); value prop = lead capture; pricing = flat tiers matching PLAN_LIMITS (Starter $29 / Pro $79 / Business $199); onboarding = full self-serve (done). **Next build = Stripe billing + plan enforcement** (new `src/billing.ts`: checkout/portal/webhook; gate sources/provider/branding by plan; dashboard billing tab) — the only thing blocking revenue. Spec has full component/data-flow/error/security/test detail. Resume = read that spec, then writing-plans skill → implement.

Earlier state (session 81e1d05a):

**Bugs fixed + verified (typecheck green, boot ok):**
1. Session memory was dead — widget now gens persistent per-tenant `sessionId` in localStorage + sends `pageUrl`; `/api/chat` passes both to `answer()`.
2. `rateLimit`/`overQuota` now wired into `/api/chat` (per-tenant+IP 429; quota blocks only NEW convos via new `sessionExists()` helper).
3. demo bots/ingest now create a `tenants` row via new `ensureTenant()`.
4. Input validation (400) on `/api/chat` + `/api/ingest`.

**Helpers added to lib.ts (compile, NOT yet wired):** `ensureTenant`, `sessionExists`, `configWarnings`, `recordBooking`, `tenantStats`, `cfg.supabaseAnonKey`.

**Decisions locked this session (user):** goal = harden + add features that make people want to pay, then revisit. Build FULL dashboard. Auth = Supabase Auth (magic-link; RLS `is_member()` policies already in schema.sql — need `SUPABASE_ANON_KEY` for browser client). Build ALL four "wiring" features.

**NEXT STEPS (resume here, in order):**
1. Wire `configWarnings()` into index.ts boot (log missing env).
2. `/api/book` endpoint + cal.com link (`tenant.booking_config.calLink`) into `answer()` system prompt.
3. Widget theming: pull `name`/`brand_color`/`logo_url`/`persona` per tenant into `/widget.js`.
4. `/api/stats` (uses `tenantStats`) + `scripts/resync.ts` (re-ingest all docs; dedup hash skips unchanged) for scheduled freshness.
5. Full dashboard + Supabase Auth: login → paste URL/ingest → copy embed → leads/convos/gap report (`unanswered_questions`, `feedback`).

Schema is AHEAD of code: `bookings`, Stripe cols, `brand_color`, `booking_backend`, `tenant_members` exist but unread — several features are wiring, not new tables.
